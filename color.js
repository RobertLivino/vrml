function color (r, g, b) {
    r = r / 255
    g = g / 255
    b = b / 255
    console.log(r.toFixed(2), g.toFixed(2), b.toFixed(2))
}

function rotation (g) {
    g = g * (3.41/180) 
    console.log(g.toFixed(2))
}